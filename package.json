{
  "name": "form-pilot",
  "private": true,
  "version": "0.1.0",
  "description": "FormPilot monorepo - AI-driven UI Agent infrastructure",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.18",
    "pnpm": ">=8.6"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean || echo ok"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.8",
    "@changesets/cli": "^2.27.8"
  }
}