{
  "name": "@fhuang/form-pilot-adapter-plain",
  "version": "0.1.0",
  "private": false,
  "description": "Plain object form adapter for FormPilot demos (no UI library)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint setup'",
    "clean": "rimraf dist || rmdir /s /q dist"
  },
  "dependencies": {
    "@fhuang/form-pilot-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  }
}